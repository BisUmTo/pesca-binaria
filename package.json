{
  "name": "pesca-binaria",
  "version": "3.0.0-preview",
  "private": true,
  "type": "module",
  "description": "Gioco statico mobile first per allenarsi con il sistema binario",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  }
}
